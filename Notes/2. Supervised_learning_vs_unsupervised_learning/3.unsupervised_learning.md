# Unsupervised Learning

- Unsupervised learning
- Idea
- Clustering
- Dimensionality Reduction
- Applications


---

### Goal of Unsupervised leanring:

- The goal of unsupervised leartning to find underlying data structure of the datasets or how we group the data most usefully stating with a unlabelled dataset.


- The two unsupervised learning techniques we will explore are clustering the data into groups by similarity and reducing dimensionally to compress the data while maintaining its structure and usefulness.


- These tasks are helpful in doing Exploratory data analysis, customer segmentation, grouping similar images etc. 


![alt text](https://i.imgur.com/tkjLK39.png)


### Clustering

The goal of clustering is to create groups of data points such that points in different clusters are dissimilar while points whithin a cluster are similar.


### Techniques:

- **K-Means Clustering**
- **Hierarchical Clustering**


### Dimensionality Reduction

This is about trying to reduce the complexity of the data while keeping as much of the relevant structure as possible. if you take a simple 128 x 128 x 3 pixel image (length x width x RGB vale), that's 49,152 dimentions of data.


if you're able to reduce the dimensionality of the space in which these images live without destroying too much of the meaningful content in the images, then you've done a good job at dimensionaltiy reduction.  

### Techniques

- **Principle Component Analysis**
- **Singular value decomposition**
- **T-SNE**
- **Deep Learning Techniques - Autoencoder**

----
Applications -

- Google photos wants to cluster similar looking faces under one category

- Clustering news articles related to one topic under one heading

- an advertising platform segments the population into smaller groups within similar purchasing habits so that adverstisers can reach their target markets

- we often need reducing number of dimentions in a large data set to simplify models. (Dimentionality reduction)

